ro.pacrom.version=${PRODUCT}-${PRODUCT_VERSION}-${DEVICE}-UNOFFICIAL
ro.pacrom.display.version=${PRODUCT}-${PRODUCT_VERSION}-${DEVICE}-UNOFFICIAL
ro.modversion=${PRODUCT}-${PRODUCT_VERSION}-${DEVICE}-UNOFFICIAL
ro.pacstats.url=http://stats.pac-rom.com
ro.pacstats.name=PAC-man
ro.pacstats.version=${PRODUCT}-${PRODUCT_VERSION}-${DEVICE}-UNOFFICIAL
ro.pacstats.tframe=1
